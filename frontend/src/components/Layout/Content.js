import React from 'react';

const Content = ({ children }) => {
    return (
        <main className="container mt-4">
            {children}
        </main>
    );
}

export default Content;
